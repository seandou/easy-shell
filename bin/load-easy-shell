#!/bin/bash

function list() {
    easy-shell "list" "$1" "$2"
}

function confirm() {
    easy-shell "confirm" "$1" "$2"
}

function result() {
    cat $HOME/.escache
}

# clear cache
cat /dev/null > $HOME/.escache
